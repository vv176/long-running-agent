# Runtime
openai>=1.40
python-dotenv>=1.0

# Tests
pytest>=8.0

# The fixture app (fixtures/miniledger) is Flask; the agent migrates it to FastAPI,
# so BOTH must be importable or the suite cannot run before and after the change.
flask>=3.0
fastapi>=0.110
httpx>=0.27          # FastAPI's TestClient needs it
pydantic>=2.0
